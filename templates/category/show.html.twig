{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<br><br><br>
    <div class="form-container">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ category.name }}</td>
                </tr>
            </tbody>
        </table>

        <a class="page-link" href="{{ path('app_category_index') }}">Retour à la liste</a>

        <a class="page-link" href="{{ path('app_category_edit', {'id': category.id}) }}">Modifier</a>

        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
