{% extends 'base.html.twig' %}

{% block title %}RatingService{% endblock %}

{% block body %}
    <h1>RatingService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rating_service.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ rating_service.value }}</td>
            </tr>
            <tr>
                <th>Published</th>
                <td>{{ rating_service.published ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rating_service_index') }}">back to list</a>

    <a href="{{ path('app_rating_service_edit', {'id': rating_service.id}) }}">edit</a>

    {{ include('rating_service/_delete_form.html.twig') }}
{% endblock %}
